题目：
给定一个链表，判断链表中是否有环。

为了表示给定链表中的环，我们使用整数 pos 来表示链表尾连接到链表中的位置（索引从 0 开始）。 如果 pos 是 -1，则在该链表中没有环。

struct ListNode {
    int val;
    struct ListNode *next;
};

bool hasCycle(struct ListNode *head) {
    struct ListNode *t[100]={0};//定义一个指针数组来存放地址
    int i=0,j;

    while(head!=NULL){
        for(j=0;j<i;j++)
            if(t[j]==head)
                return true;
        t[i]=head;
        head=head->next;
        i++;
    }

    return false;
    
}
